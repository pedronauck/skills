---
type: <bug-fix | improvement | feature>
created: <YYYY-MM-DD HH:mm>
source: <repo · branch · issue/PR refs, when they exist>
---
# <One line: the problem and the outcome wanted>

## Problem

<!-- What is wrong or missing, and the outcome wanted — no solution.
     bug-fix: current vs. expected behavior; who/what is affected and how severely.
     improvement: the specific aspects needing improvement (performance, maintainability, usability…) and how users benefit.
     feature: complete functional requirements and user stories. -->

## Current State

<!-- How the system works today. Quote real code as evidence, cite file:line.
     bug-fix: the implementation of the affected code paths and the related components that interact with them.
     improvement: the existing approach being improved, with related code and dependencies.
     feature: where the feature fits, its integration points, and the current data models and API contracts it touches. -->

## Evidence

<!-- Cite concrete facts; use short verbatim excerpts only where exact wording matters.
     bug-fix: exact reproduction steps; relevant error/log excerpts without credentials or unrelated private data; environment (OS, runtime, dependency versions, config); recent changes that may have introduced it.
     improvement / feature: measurements, examples, or reports that motivate the work. -->

## Requirements

<!-- What must be true when the work is done — preserve accepted implementation constraints and approved plans.
     bug-fix: the expected behavior, stated testably; regression coverage that pins the fix.
     improvement: success criteria — how to measure that the improvement landed.
     feature: user flows; edge cases and boundary conditions; new data or API needs stated as requirements, not designs. -->

## Constraints & Non-goals

<!-- Technical, business, or design limits; backward-compatibility requirements; external dependencies; what stays untouched. -->

## Verification

<!-- Name the owning suite or probe for each changed invariant; reuse existing coverage and avoid duplicating it across levels. -->

## References

<!-- Files (path:line), issues, PRs, docs, links the receiver should open. -->
